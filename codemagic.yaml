    workflows:
      saeed-notes-ios:
        name: Saeed Notes iOS Build
        environment:
          flutter: stable
        scripts:
          - script: flutter pub get
          - script: flutter build ipa --release --no-codesign
        artifacts:
          - build/ios/ipa/*.ipa
